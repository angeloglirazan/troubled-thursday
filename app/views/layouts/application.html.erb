<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Troubled Thursdays</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">

  <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!--[if lt IE 9]>
    <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "bootstrap-generators" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  <% if ENV["GA_TRACKING_ID"] %>
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', '<%= ENV["GA_TRACKING_ID"] %>', 'auto');
      ga('send', 'pageview');
    </script>
  <% end %>

</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
    <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Troubled Thursdays</a>
      </div>
  
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="tt-navbar-collapse">
        <ul class="nav navbar-nav">
          <li<%= ' class="active"' if params[:controller] == "questions" && params[:action] == "index" %>><%= link_to "Questions", questions_path %></li>
          <li<%= ' class="active"' if params[:controller] == "questions" && params[:action] == "new" %>><%= link_to "Ask a Question", new_question_path %></li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  <div class="container">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, :class => "alert #{name == :error ? "alert-error" : "alert-success" } alert-dismissable" do %>
        <button class="close" data-dismiss="alert" href="#"><span aria-hidden="true">&times;</span></button>
        <%= msg %>
      <% end %>
    <% end %>
    <%= yield %>
    <hr>
    <!--<footer>
      <p>
        <small>Maintained by: TroubledThursdays -at- Gmail.com</small><br>
        <small>Development by: Angelo.Lirazan -at- Gmail.com</small>
      </p>
    </footer> -->
  </div>
</body>
</html>
