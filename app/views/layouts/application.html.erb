<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Troubled Thursdays</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">

  <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!--[if lt IE 9]>
    <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->

  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "bootstrap-generators" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <div class="brand">Troubled Thursdays</div>
        <div class="nav-collapse">
          <ul class="nav">
            <li<%= ' class="active"'.html_safe if params[:controller] == "questions" && params[:action] == "index" %>><%= link_to "Questions", questions_path %></li>
            <li<%= ' class="active"'.html_safe if params[:controller] == "questions" && params[:action] == "new" %>><%= link_to "Ask a Question", new_question_path %></li>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, :class => "alert #{name == :error ? "alert-error" : "alert-success" } alert-dismissable" do %>
        <button class="close" data-dismiss="alert" href="#"><span aria-hidden="true">&times;</span></button>
        <%= msg %>
      <% end %>
    <% end %>
    <%= yield %>
    <hr>
    <footer>
      <p>&copy; Company 2012</p>
    </footer>
  </div>
</body>
</html>
